@model IEnumerable<HarmonyHotles.Models.Roomtype>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Typename)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Roomtypesdescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Roomsize)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Typename)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Roomtypesdescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Roomsize)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Roomtypeid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Roomtypeid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Roomtypeid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
